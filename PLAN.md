# newsalpha — мастер-план v3.0 (24.08.2026)

Открытая реализация метода **STTM** (PeerJ CS 2022, DOI 10.7717/peerj-cs.1156) и его
первоисточника **SESTM** (Ke/Kelly/Xiu: BFI 2019 → JASA 2026) на российском рынке.
Версия v3.0 написана заново после отклонения артефактов стороннего исполнителя;
исследования лежат в `reports/` (4 документа), они — фактическая база этого плана.

**Цель:** воспроизвести результаты статьи на MOEX TQBR + новостях 2013–2021, пройти
три красных пункта критики (**нетто-доходность после издержек**, **поправка на
множественный отбор**, **воспроизводимость**) и выпустить проверенный референс-имплемент.

---

## 1. Принципы архитектуры

1. **Данные прежде всего** — никакого модельного кода до сборки корпуса и EDA.
2. **Стадии общаются файлами** — каждый этап читает/пишет артефакты в `data/` и
   `models/`, перезапускается независимо.
3. **Без утечек** — сигнал считается только на train-окне; expanding CV.
4. **Всё измеряется** — gross И net-Sharpe; отчёт по всем моделям без отбора лучших;
   10 сидов → mean±std.
5. **Конфиг-driven** — `config/default.yaml` единственный источник гиперпараметров.

## 2. Структура проекта

```
newsalpha/
├── PLAN.md                    ← этот файл
├── README.md                  ← быстрый старт
├── requirements.txt           ← зависимости
├── config/default.yaml        ← все гиперпараметры
├── data/
│   ├── raw/{news,prices,riskfree}/   # сырьё; цены TQBR уже в raw/prices
│   ├── interim/               # препроцессинг, дедупликация
│   └── processed/             # doc-term матрицы, панели сигнал×тикер×неделя
├── src/newsalpha/             # пакет: io, text, topics, sttm, baselines,
│                              #        evaluation, strategy, viz
├── scripts/                   # тонкие argparse-CLI по стадиям + run_all.py
├── notebooks/                 # EDA (не продакшн)
├── tests/                     # pytest: каждая формула ядра STTM
├── reports/                   # исследования (method_critique, sestm_original_review,
│                              #            related_projects, plan_review_external)
└── models/                    # артефакты моделей (gitignore)
```

**Поток:** `scripts/fetch_* → data/raw → preprocess.py → interim → train_topics.py +
build_index.py → processed → backtest.py → reports/`

**CLI (решение зафиксировано):** гибрид — тонкие argparse-скрипты по стадиям, каждый
читает `--config config/default.yaml`; оркестрация плоским `run_all.py`.

## 3. Зафиксированные решения

| Вопрос | Решение | Обоснование |
|---|---|---|
| Python | 3.10+, venv, `pip install -e .` | стандарт |
| LDA vs DTM | **gensim≥4.3.3, LDA-only дефолт** | 4.3.0–4.3.2 несовместимы с numpy 2.x; DTM (gensim 3.8) — отдельный venv через WSL/Docker, опционально |
| Лемматизатор | **pymystem3** + NLTK stopwords | подтверждено патентом ВТБ EA044248B1 |
| Токенизация / NER | razdel; склейка имён `_` через natasha | как в сэмплах авторов |
| Метрики портфеля | quantstats | empyrical архивирован |
| Корпус | **готовые корпуса первыми** (ODS Interfax 544k, Lenta экономика), скрапинг Ъ вторым этапом | быстрый старт без риска антибота |
| Издержки | сценарии комиссии 0 / 0.05% / 0.15% + slippage 0.1%; **KPI — net Sharpe** | красный пункт №1 критики |
| Сиды | 10 сидов LDA → mean±std везде | нестабильность тем (статья так же) |

## 4. Этапы

### Этап 0. Окружение 🔄
- [x] venv на **Python 3.11** (uv-cpython 3.11.15; системный 3.14 не годится — нет wheel'ов
      у научного стека) → `pip install -r requirements.txt` + `pip install -e .` ✅ 24.08
- [x] NLTK stopwords RU (151 шт.) — требуется `NLTK_ALLOW_PROXIED_URLOPEN=1` из-за прокси
- [x] pymystem3 работает (MyStem 3.1 скачан автоматически); пин в requirements
      исправлен на `pymystem3>=0.2`
- [ ] git init + первый коммит (по команде владельца)

### Нюансы окружения (обнаружено 24.08)
- Lenta v1.0 CSV **не содержит колонки даты** — дата извлекается из URL `/news/YYYY/MM/DD/`
  (реализовано в `src/newsalpha/io/fetchers.py`); записи идут по убыванию даты.
- **Корпус ODS (interfax/gazeta/iz/meduza/ria/tass) потерян**: репозиторий
  ods-ai-ml4sg/proj_news_viz удалён с GitHub (404), в Wayback Machine только
  страницы-редиректы, зеркал на HF/Kaggle нет. Interfax недоступен.
- **ria.json.gz бесполезен для нас**: в записях только title/text (corus.RiaRawRecord),
  нет date/url — тайм-алигнирование событий невозможно. Исключён из конфига.
- **Lenta v1.0 обрывается на 2018-12-15** — годы 2019–2021 закрывает только скрапинг Ъ.

### Этап 1. Данные 🔄 (строго первый содержательный этап)
- [x] 1a-инфраструктура: `scripts/fetch_news.py` + `src/newsalpha/io/fetchers.py`
      (стриминговое скачивание → фильтр дат/рубрик → parquet-парты в interim) ✅ smoke на Lenta
- [x] 1a. Полный прогон Lenta: **739 351 просмотрено → 86 587 экономических новостей**
      (1999-10…2018-12; по ~4–8k/год, экономика+бизнес+финансы) → `data/interim/news_lenta_part000.parquet`
      ✅ 24.08. Interfax отменён (корпус потерян), RIA исключена (нет дат).
- [ ] 1b. Скрапинг Коммерсанта `kommersant.ru/archive/rubric/3/day/{YYYY-MM-DD}` 2013–2021 —
      **критический путь** (единственный источник 2019–2021 + основной источник статьи).
      Разведка 24.08: архив дня отдаёт JSON-LD ItemList (title+url всех статей, пагинации нет,
      ~27 статей/день); у статьи `article:published_time` с точностью до минуты,
      полный текст в `div.article_text_wrapper` (~4k символов), пейволла на старых статьях нет,
      доступен plain requests с UA. Оценка объёма: ~3300 дней × ~30 статей ≈ 100k запросов,
      при задержке 0.5–0.7 c ≈ сутки непрерывного скрапинга → нужен resume по дням.
- [x] 1b-smoke: `src/newsalpha/io/kommersant.py` + `scripts/fetch_kommersant.py`
      (union ItemList∪href + фильтр published_time по дню; resume по дневным JSONL;
      retry с бэкоффом). Прогон 2019-01-07…27: **21 день, 0 сбоев, 175 статей**
      (~8/день; январь праздничный). Полный прогон 2012-12…2021-12 — по команде.
- [x] 1-тесты: `tests/test_parsers.py` — 10 тестов парсеров Ъ/Lenta на реальных
      HTML-fixture (`tests/fixtures/ka_*.html`); регрессия парсера = битые даты = шум меток.
- [ ] 1c. Цены: уже есть 39 TQBR CSV; продление 2022–2026 через MOEX ISS API
- [ ] 1d. Единый формат `(date, source, title, text, url, rubric)` + дедупликация
      (hash заголовка + fuzzy >0.95) — контрмера к замечанию №5 критики
- [ ] 1e. EDA-ноутбук: покрытие по тикерам/годам; разбор провала 2018 по источникам

### Этап 2. Препроцессинг (`src/newsalpha/text`) ✅ 24.08
`src/newsalpha/text/preprocess.py` + `scripts/preprocess_news.py`.
razdel → фильтр токенов без букв → леммы mystem (файловый батч-режим бинарника:
pymystem3 на Windows перезапускал процесс на строку — 27 мс/токен → 0.03 мс/токен)
→ NLTK RU stopwords → дефисы→«_» → склейка NER-спанов natasha (PER/ORG/LOC).
Пул 10 процессов. Полный прогон: **Lenta 86 587 доков за 491 c** (медиана 125
токенов/док), Ъ 215 доков; словарь Lenta 210 878 → 200 362 (idf-квантили DF).
Сверка с авторскими сэмплами Ъ: Жаккар 0.57–0.75 (отклонение задокументировано
в модуле: их колонка уже после idf-фильтра + недовосстановимая склейка ADJ+NOUN).
Известный шум: ~5% мусорных краевых токенов («—visa», «113_e») — низовой DF-порог
не срабатывает при Zipf; уберётся no_below/no_above при построении словаря gensim.
8/215 статей Ъ без тела текста (анонсы/фото) — остаются с заголовком.

### Этап 3. Тематические модели (`src/newsalpha/topics`) ✅ 24.08
`src/newsalpha/topics/lda_model.py` + `scripts/train_topics.py` (LdaMulticore,
seed=11, passes=10, словарь no_below=10/no_above=0.4 → 19 864 слова).
Грид C_v по k∈{2..50, шаг 3}, 17 фитов за 61 мин:
пик **k=32 (C_v=0.567)**; плато 26–47 (все ≥0.53 — «плато, а не пик», п. №3);
k=20 из статьи = 0.532 (на подъёме кривой). Выбор k=32 как data-driven;
сопоставление с k=20 — в грид чувствительности Этапа 6.
doc-topic матрица → `models/doc_topic/doc_topic_lenta.parquet`.
Один сид; финальные числа — mean±std по 10 сидам на полном протоколе.

### Этап 4. Ядро STTM (`src/newsalpha/sttm`) ✅
Порт функций `sttm.py` авторов под gensim 4:
1. topic stream Θ[j,t]; 2. word stream c[w,t]; 3. тон слова f_w = pearson r при p<γ=0.05 иначе 0;
4. тональность темы: топ-слова до массы 0.3, f_T = pProb − nProb (0, если ни одно слово незначимо);
5. TTS = Θ·f_T; 6. индекс = ΣTTS → сигмоид → [0,1]. Только на train-окне (без утечек).
Расширения патента EA044248B1: logreg на знаке доходности вместо Пирсона; ITMTF опционально.
**Готово:** core.py (10 тестов) + pipeline.py (build_streams, sttm_expanding — expanding-CV
по календарным годам) + word_tone_matrix (векторный аналог word_tone, эквивалентность 1e-9)
+ **5 тестов sttm_expanding вкл. анти-утечку** (двойной цикл P>=C invariant, P<C sensitive).
scripts/build_sttm_index.py → models/sttm_indices/sttm_index_lenta.parquet
(39 тикеров, тест-годы 2015–2018 = 208 недель строго по протоколу).
first_test_year теперь глобальный параметр (config→evaluation.protocol_start + initial_train_years),
обязательный аргумент sttm_expanding — убрана маскирующая дефолт-None (recheck Mavis 2026-08-25).
**Ревизия 25.08.2026:** баг осей (stock_index получал [темы×недели] вместо [недели×темы])
и тест-годы начинались с недоученного 2014 — вскрыто синтетическими тестами на утечку;
результаты отозваны и перепрогнаны.
Коммит 9da3618 (25.08.2026): pipeline вызывает core.tts() вместо дублирования формулы,
F821 в kommersant.py исправлен, тесты пересобраны с сильным сигналом (20 слов, 2 темы).
Диагностика на Lenta (исправленная): предиктивная сила ≈ 0 и ниже случайного ранжирования
(z ≈ −2) — ожидаемо для нецелевого корпуса (вся экон-лента ≠ рубрика Ъ «Финансы»).

### Этап 5. Базлайны (`src/newsalpha/baselines`) ⬜
- **SESTM** — пишем сами по официальному MATLAB-коду JASA-supplement
  (screening f_i ≥ thresh·log N; оценка O регрессией на ранговых весах + симплекс).
  Битый sestm.py авторов не чиним.
- **Shallow embeddings**: Word2Vec/Navec/Doc2Vec/FastText × RF/LR/GBM/SVM/NN
  (full text / title / abstract — 20 комбинаций).
- **Эндогенные**: 5 лагов доходности × те же 5 ML — обязательная атрибуция.
- ruBERT/RuSentBERT эмбеддинги → Ridge/LogReg — контрмера к замечанию Громова.

### Этап 6. Оценка (`src/newsalpha/evaluation`) ✅ (ядро)
Грейнджер maxlag=5 (+ADF); **placebo ±k недель** (заготовка, требует rebuild streams);
**Benjamini-Hochberg FDR** для доли значимых акций; абляция горизонта 1/2/4 недели;
метрики направления Acc/F1/AUC/Spearman; **полный кросс-секционный грид чувствительности**
{γ}×{prob_mass} — критерий «плато, а не острый пик».
**Готово:** granger.py, direction.py, fdr.py, sensitivity.py (per-ticker sttm_expanding →
кросс-секционный backtest top-20%) + run_evaluation.py + 10 тестов.
Результат на Lenta (25.08.2026): Granger lag1 p=0.008 (значим), Direction Acc=0.42
(случайный 0.50 — индекс контринтуитивен, Spearman −0.12), FDR 0/38 после BH,
Sensitivity **PLATEAU** (Sharpe 0.60–0.88, нет острого пика; топ γ=0.08 pm=0.40 →
gross=0.876 net=0.846). Placebo (100 сидов) — запускать отдельно, требует rebuild theta/c_words.

### Этап 7. Стратегия (`src/newsalpha/strategy`) ✅ (ядро)
Long-only топ-20%, недельная ребалансировка. Комиссии {0; 0.05%; 0.15%} + slippage 0.1%;
ребаланс только при смене топ-N (снижение оборота). SESTM-практики: фильтр ночных
новостей (исполнение на открытии), микро-капы breakpoint, novelty-фильтр повторов.
Бенчмарки: IMOEX, EWI, случайный сигнал; rf = ОФЗ (ЦБ РФ).
Опция (опыт isomiki): **per-ticker сигмоид-калибровка** expit(a·signal+b),
a/b подбираются логрегрессией на train-окне.
**Готово:** backtest/portfolio.py (9 тестов: равные веса, оборот только по Δw,
издержки rate×Σ|Δw|, object/None-защита) + scripts/run_backtest.py.
Результат на Lenta-индексах после ревизии 25.08.2026 (тест 2015–2018, 207 недель):
gross level Sharpe **0.68** (ret +11.8%), delta **0.43**; net_0.05% level Sharpe **0.66**;
случайный сигнал — медиана Sharpe 0.96 [0.49..1.37] (100 сидов), равновзвеш. рынок 0.85 →
STTM ниже случайного, z ≈ −2. Чекпойнт статьи (1.37) не воспроизводится; на нецелевом
корпусе сигнал не просто нулевой, а слегка вредоносный. Оборот ~0.97/нед → net_0.15%
уводит delta в минус.
Решающий аргумент за прогон на Ъ: либо воспроизведение ~1.37 валидирует пайплайн,
либо независимая реализация опровергает результат статьи. Оба исхода фиксируем.
(Отозванные числа первой версии — level 1.00 / delta 1.06, z≈+1.3 — продукт бага осей.)

### Этап 8. Воспроизводимость ⬜ (в процессе)
`run_all.py` одной командой; фиксация random_seed в `config/default.yaml`;
CI: ruff (E402 в scripts допускается) + pytest.

### Этап 9. Расширение 2022–2026 «метод после разлома» ⬜ (решение владельца, авг 2026)
Ядро проекта остаётся 2013–2021 (воспроизведение); этот этап — отдельный out-of-sample
эксперимент на структурном разломе рынка. Инфраструктура уже умеет любые даты.
1. Цены: докачка ISS MOEX до текущего дня (`data/raw/prices`, одна выгрузка).
2. Вселенная: пересмотр списка тикеров — из 39 бумаг статьи половина исчезла
   (YNDX, QIWI, FIVE, RUAL, POLY делистинг/уход); февраль–март 2022 = месяц без торгов,
   NaN-недели обрабатываются рыночным модулем. Фиксировать состав вселенной и даты входа.
3. Новости: скрапинг Ъ 2022–2026 (~+2.5 ч/год поверх основного прогона).
4. Прогон: тот же пайплайн (этапы 2–7) без перенастройки гиперпараметров —
   параметры с train-окна ядра, никакого подглядывания в 2022+.
5. Интерпретация: сигнал живёт после разлома → аргумент за метод;
   умирает → честный красный флаг в критику статьи. Оба исхода публикуем.

### Этап 10. Мультисорс: расширение числа источников ⬜
Почему сейчас один источник: (а) верность воспроизведению — авторы работали только
с Ъ; (б) корпуса ODS (interfax/gazeta/iz/tass/meduza) удалены с GitHub навсегда,
у РИА нет дат в записях. Критерии пригодности источника (по убыванию): точная дата
статьи → полные тексты → архив ≥2013 без пейволлы → экономический фокус
(экспериментально доказано на Lenta: общая лента разбавляет сигнал до нуля) →
парсимость. Кандидаты и зонды (~30 мин каждый):
- **RBC** — эконом-рубрики, даты, открытые тексты, глубокий архив;
- **Interfax.ru** (сам сайт, не потерянный датасет) — ежедневные финанс-ленты;
- **Ведомости** — профильная газета, но старые номера за пейволлой (проверить глубину).
Методическая ценность: у авторов один источник; устойчивость сигнала к выбору
источника никто не проверял — это наше расширение. При конфлюенсе источников —
дедупликация по дате±косинусной близости текстов.

### Этап 11. Соцмедиа: Telegram / Max ⬜ (пилот после Этапа 10)
Идея владельца: современный инвестор читает профильные каналы, а не только газеты.
- **Telegram** — реалистичный кандидат, но три серьёзных оговорки:
  1) архив: публичные каналы читаются на всю историю через MTProto (Telethon),
     однако финTG-экосистема взросла с ~2017–2018 → для 2013–2016 покрытия почти нет;
     ценность — периоды 2019+ и расширение 2022–2026;
  2) смещение выжившего: перечислить можно только дожившие каналы; удалённые
     исчезли из вселенной навсегда;
  3) шум и adversaries: репосты, реклама, памп-каналы (их посты двигают цены
     злокозненно — для метода это яд, нужна ручная белая список из ~10–30
     добросовестных экономических каналов); короткие посты ≠ газетный текст —
     LDA по коротким текстам слабее, возможно нужен отдельный препроцессинг.
  Пилот: Telethon-зонд по 3–5 крупным каналам — доступная глубина истории,
  рейт-лимиты, качество текста (~полдня).
- **Max** — пока не источник данных: закрытая платформа без публичного API поиска/
  истории, финконтент минимален. Мониторить появление публичного доступа.
Не входит в ядро воспроизведения; идёт как расширение критической части работы.

## 5. Протокол экспериментов

1. **10 сидов** → mean±std для каждой ячейки результатов.
2. **Expanding CV**: train 2013–2014, тест следующий год … до 2021 (6 сплитов).
3. Отчёт по **всем** моделям и источникам — без выборки лучших (анти-snooping).
4. Каждая гипотеза проходит placebo и сравнение с эндогенным базлайном.
5. Робастность: дополнительно rolling-window CV (train 5 лет → val 1 → test 1,
   сдвиг на год) как проверка expanding-схемы на чувствительность к окну.

## 6. Чекпойнты воспроизведения

| Чекпойнт | Ожидание (статья) |
|---|---|
| C_v пик числа тем | ~20 для всех источников |
| Sharpe STTM(LDA) Коммерсантъ | 1.3706 ± 0.0907 / 36.12% ± 2.12% |
| Доля акций со значимым Грейнджером | до 70% к 2020, провал 2018 |
| SESTM-базлайн | ≤ STTM (в статье максимум 0.64) |

## 7. Риски

| Риск | Уровень | Митигация |
|---|---|---|
| Полный корпус статьи не опубликован | 🔴 | готовые корпуса + скрапинг Ъ; честная фиксация coverage |
| Антибот Коммерсанта | 🟡 | uc-chromedriver, троттлинг, прокси, буфер времени |
| Data snooping | 🔴 | отчёт по всем, BH-FDR, placebo |
| Net-Sharpe после издержек | 🔴 | издержки с первого прогона, KPI net |
| Новостной сигнал слаб на малых горизонтах | 🟡 | недельный горизонт, эндогенный базлайн обязателен |

## 8. Что НЕ делаем

- ❌ Не упрощаем индекс до прямой корреляции тем с доходностью (ошибка marin-shartey).
- ❌ Не чиним битый `sestm.py` авторов — свой порт по официальной спеке.
- ❌ Не выбираем лучшие результаты постфактум.
- ❌ Не трогаем настройки opencode (`context_length` и пр.) — запрещено владельцем.

## 9. Исследовательская база

- `reports/method_critique.md` — 20 недостатков метода, красные пункты и контрмеры
- `reports/sestm_original_review.md` — разбор официального кода SESTM, что переносим
- `reports/related_projects.md` — экосистема GitHub (⚠️ isomiki/sttm-trader — тот же рынок)
- `reports/plan_review_external.md` — ревью внешнего плана и отклонённого исполнителя

## 10. Первоисточники

STTM: peerj.com/articles/cs-1156 · Рябых, диссертация: hse.ru/sci/diss/1087526075 ·
EPJ DS 2025: doi 10.1140/epjds/s13688-025-00535-z · SESTM JASA 2026:
doi 10.1080/01621459.2026.2643001 · код SESTM: doi 10.6084/m9.figshare.31825294.v2 ·
патент EA044248B1: patents.google.com/patent/EA044248B1/ru
